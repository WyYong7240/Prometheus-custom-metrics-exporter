apiVersion: v1
kind: Service
metadata:
  name: hardware-exporter-external
  namespace: monitoring
  labels:
    app: hardware-exporter
spec:
  type: NodePort
  ports:
    - name: http-metrics
      port: 30012
      targetPort: 30012
      protocol: TCP